# Neural Network with Manual Backpropagation

Implementation of a 2-layer feedforward network from scratch using NumPy.

## Requirements
Install dependencies using:
```bash
pip install -r requirements.txt
```

## Running the Model

```bash
python model.py
